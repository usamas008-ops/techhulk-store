# Copy this file to .env.local for local dev, and add the same values
# in Vercel -> Project Settings -> Environment Variables for production.

NEXT_PUBLIC_SUPABASE_URL=https://eaogyiwdbrpqfifvvzjo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_LkHRUtm2INPSvftMVH-YYQ_bokXJGHu

# Only needed for the one-time product import script (scripts/import-products.mjs).
# This is the SECRET key from Supabase -> Settings -> API -> Secret keys.
# NEVER commit this, never put it in NEXT_PUBLIC_* variables, never expose it to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Store whose catalog we import from (public Shopify products.json endpoint)
SOURCE_STORE_URL=https://techhulk.store

# Optional: WhatsApp number for the floating chat button, digits with
# country code and no plus sign, for example 923001234567.
NEXT_PUBLIC_WHATSAPP_NUMBER=
